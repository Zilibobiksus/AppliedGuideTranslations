---
navigation:
  parent: ae2-mechanics/ae2-mechanics-index.md
  title: Импорт, экспорт и хранилище
  icon: storage_bus
---

# Импорт, экспорт и хранилище

**ваша система ME и мир**

Важная концепция в AE2 — это идея сетевого хранилища. Это место, где хранятся содержимое сети,
обычно [ячейки хранения](../items-blocks-machines/storage_cells.md) или любой инвентарь, к которому подключен <ItemLink id="storage_bus" />.
Большинство [устройств](../ae2-mechanics/devices.md) AE2 взаимодействуют с ним тем или иным способом.

Например,

*   <ItemLink id="import_bus" /> толкают вещи в сетевое хранилище
*   <ItemLink id="export_bus" /> тянут вещи из сетевого хранилища
*   <ItemLink id="interface" /> и тянут, и толкают в сетевое хранилище
*   [Терминалы](../items-blocks-machines/terminals.md) и толкают, и тянут из сетевого хранилища, когда вы вставляете или забираете предметы, или для пополнения слотов крафта
*   <ItemLink id="storage_bus" /> не совсем толкают или тянут из хранилища, они толкают или тянут из подключенного инвентаря,
    чтобы использовать его как сетевое хранилище (поэтому на самом деле другие устройства толкают или тянут из *них*)

<GameScene zoom="4" interactive={true}>
  <ImportStructure src="../assets/assemblies/import_export_storage.snbt" />

  <BoxAnnotation color="#dddddd" min="8 1 1" max="9 1.3 2">
        Шины импорта импортируют вещи из инвентарей, на которые они направлены, в сетевое хранилище
  </BoxAnnotation>

  <BoxAnnotation color="#dddddd" min="8 2 1" max="9 3 1.3">
        Вставка чего-либо в терминал из вашего инвентаря считается импортом сети
  </BoxAnnotation>

  <BoxAnnotation color="#dddddd" min="7 0 1" max="8 1 2">
        Интерфейсы будут импортировать из своих внутренних инвентарей, если этот слот не настроен на пополнение чем-либо, или если в этом слоте больше
        предметов, чем настроено для пополнения, поэтому вещи можно толкать в них для вставки в сеть
  </BoxAnnotation>

  <BoxAnnotation color="#dddddd" min="6 0 1" max="7 1 2">
        Поставщики шаблонов будут импортировать из своих внутренних инвентарей возврата, поэтому вещи можно толкать в них для вставки в сеть
  </BoxAnnotation>

  <BoxAnnotation color="#dddddd" min="4 1 1" max="5 2 2">
        Приводы предоставляют вставленные ячейки в качестве сетевого хранилища
  </BoxAnnotation>

  <BoxAnnotation color="#dddddd" min="3 1 1" max="4 1.3 2">
        Шины хранения используют инвентарь, на который они направлены, в качестве сетевого хранилища
  </BoxAnnotation>

  <BoxAnnotation color="#dddddd" min="1 1 1" max="2 1.3 2">
        Шины экспорта экспортируют вещи из сетевого хранилища в инвентари, на которые они направлены
  </BoxAnnotation>

  <BoxAnnotation color="#dddddd" min="1 2 1" max="2 3 1.3">
        Извлечение чего-либо из терминала считается экспортом сети
  </BoxAnnotation>

  <BoxAnnotation color="#dddddd" min="0 1 1" max="1 2 2">
        Интерфейсы будут экспортировать в свои внутренние инвентари, если этот слот настроен на пополнение чем-либо,
        поэтому вещи можно вытаскивать из них для извлечения из сети
  </BoxAnnotation>

  <IsometricCamera yaw="195" pitch="30" />
</GameScene>

Действия/события толкания в и вытягивания из сетевого хранилища важно помнить при проектировании автоматизации
и логистических настроек.

## Приоритет хранилища

Приоритеты можно установить, щелкнув гаечным ключом в правом верхнем углу некоторых GUI.
Предметы, поступающие в сеть, будут начинаться с хранилища с наивысшим приоритетом,
в качестве их первого назначения, в случае, если два хранилища имеют одинаковый приоритет,
если одно уже содержит предмет, они предпочтут это хранилище любому
другому. Любые ячейки в белом списке будут рассматриваться как уже содержащие предмет
при нахождении в той же группе приоритета, что и другие хранилища. Предметы, удаляемые из хранилища, будут
удалены из хранилища с наименьшим приоритетом. Эта система приоритетов означает, что по мере вставки и удаления предметов
из сетевого хранилища хранилища с более высоким приоритетом будут заполняться, а хранилища с более низким приоритетом будут опустошаться.